# AGENTS.md

## Cursor Cloud specific instructions

This repository (`awards_interpreter_120`) is currently empty — it contains only a `README.md` and `LICENSE` (MIT). No application code, dependency manifests, build configs, or services exist yet.

### Current state

- **Language/Framework**: Not yet chosen.
- **Package manager**: None configured.
- **Services**: None to start.
- **Tests**: None to run.
- **Lint**: No linter configured.

### For future agents

Once application code is added, update this section with:
- How to install dependencies (e.g. `npm install`, `pip install -r requirements.txt`).
- How to run the dev server, tests, and linter.
- Any non-obvious environment setup requirements.
